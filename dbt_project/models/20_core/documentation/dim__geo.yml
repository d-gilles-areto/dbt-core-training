models:
  - name: dim__geo
    description: "Konforme Geografie-Dimension: Nation + Region konsolidiert."
    config:
      tags:
        - "tpch"

    columns:
      - name: nation_id
        description: '{{ doc("nation_id") }}'
        data_tests:
          - unique
          - not_null

      - name: nation
        description: '{{ doc("nation") }}'

      - name: region
        description: '{{ doc("region") }}'