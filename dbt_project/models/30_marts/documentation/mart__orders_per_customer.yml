models:
  - name: mart__orders_per_customer
    description: '{{ doc("mart__orders_per_customer__model") }}'
    config:
      tags:
        - "tpch"

    columns:
      - name: customer_id
        description: '{{ doc("customer_id") }}'
        config:
          tags:
            - "pii"
        data_tests:
          - not_null

      - name: customer_name
        description: '{{ doc("customer_name") }}'
        config:
          tags:
            - "pii"

      - name: market_segment
        description: "Marktsegment"


      - name: total_orders
        description: "Gesamtanzahl der Bestellungen"
        data_tests:
          - not_negative